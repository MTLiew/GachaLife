[build]
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "python -m playwright install-deps chromium && python -m playwright install chromium && uvicorn main:app --host 0.0.0.0 --port $PORT"